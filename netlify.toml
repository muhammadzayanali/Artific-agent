[build]
  command = "npm run build"

[build.environment]
  NODE_VERSION = "22"
  NEXT_TELEMETRY_DISABLED = "1"

# Next.js is auto-adapted via Netlify OpenNext. Set DJANGO_API_URL in Site
# settings (Production + Deploy Previews) to your live Django API URL.
